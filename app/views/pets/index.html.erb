
<h2>All the pets!!</h2>

<ul>
  <% @pets.each do |pet| %>
    <li> <%= cl_image_tag pet.photo, height: 300, width: 400, crop: :fill %>
      <%= pet.name %> </li>
      <%= link_to "Details", pet_path(pet) %>
    <% if policy(pet).destroy? %>
    <% end %>
  <% end %>
</ul>




